# Web2RU environment variables (example)
#
# Create your own `.env` (not committed) and fill values as needed.
# Web2RU loads env vars in this order:
# 1) existing environment variables
# 2) .env in current working directory
# 3) .env in repo root
#
# Required for LLM translation (OpenAI):
OPENAI_API_KEY=your_openai_api_key_here

# Optional: override default model and reasoning profile.
# Default model is defined in CLI as `gpt-5.1` (can be overridden via `--model`).
WEB2RU_MODEL=gpt-5.1
WEB2RU_REASONING_EFFORT=medium

# Optional: parsing/rendering knobs.
WEB2RU_SHADOW_DOM=auto
WEB2RU_ALLOW_EMPTY_PARTS=on

# Optional: cache directory (defaults to platform user cache dir, e.g. ~/Library/Caches/web2ru on macOS).
# WEB2RU_CACHE_DIR=/absolute/path/to/cache

# Optional: rate limit for openai.com rendering (ms between requests to the domain).
WEB2RU_OPENAI_RATE_LIMIT_MS=2500

